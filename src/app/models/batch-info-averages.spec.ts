import { BatchInfoAverages } from './batch-info-averages';

describe('BatchInfoAverages', () => {
  it('should create an instance', () => {
    expect(new BatchInfoAverages()).toBeTruthy();
  });
});
